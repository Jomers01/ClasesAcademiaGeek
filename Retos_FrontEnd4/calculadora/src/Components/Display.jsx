import React from 'react';
import "../styles/Display.css";

const Display = ({view}) => {
    return(
        <div className="Display">
            {view}
        </div>
    );
}

export default Display
