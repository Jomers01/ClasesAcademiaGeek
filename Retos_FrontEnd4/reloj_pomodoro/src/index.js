//Dependencias
import React from 'react';
import ReactDOM from 'react-dom';
import AppPomodoro from './components/AppPomodoro';

ReactDOM.render(
  <React.StrictMode>
    <AppPomodoro />
  </React.StrictMode>,
  document.getElementById('root')
);
