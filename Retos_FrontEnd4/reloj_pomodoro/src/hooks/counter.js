export const counterUP = (state, setState)=> {
    return setState(state + 1)
}

export const counterDown = (state, setState)=> {
    return setState(state - 1)
}

// export const timer = (min, sec)=> {
//     let minutos = Math.floor(min / 60)
// }